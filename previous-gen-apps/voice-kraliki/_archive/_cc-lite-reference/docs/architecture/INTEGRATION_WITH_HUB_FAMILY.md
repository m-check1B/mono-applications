# Integration with Hub Family

This project integrates with Stack 2025 components via vendor packages in `vendor/`.

Relevant packages:
- @unified/* and @stack-2025/* under vendor/packages

Integration endpoints are exposed via tRPC and selected REST routes.

