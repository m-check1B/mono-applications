# Production Deployment Guide

The full production deployment guide is maintained at the repository root:

- PRODUCTION_DEPLOYMENT_GUIDE.md

For quick start with Docker Compose:

```
docker compose -f infra/docker/production.yml up -d
```

